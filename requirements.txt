moviepy>=2.0.0
Pillow>=9.0.0
numpy>=1.24.0

# No external API is required for parsing, prompt export, or video rendering.
# Add an optional handwritten CJK .ttf/.otf font under assets/fonts/ for custom typography.
